import { NotExistUsernameDirective } from './not-exist-username.directive';

describe('NotExistUsernameDirective', () => {
  it('should create an instance', () => {
    const directive = new NotExistUsernameDirective();
    expect(directive).toBeTruthy();
  });
});
